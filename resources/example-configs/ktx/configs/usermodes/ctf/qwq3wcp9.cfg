// qwq3wcp9 was mapped in the mind that players should spawn on bases, so k_ctf_based_spawn is a MUST.
set k_ctf_based_spawn 1
// No need for hook on that map.
set k_ctf_hook 0
// Runes are way too powerful ATM and they spawn on info_player_deathmatch which present only on the RED base, nice?
// Actually I fixed runes and they spawns on the bases with k_ctf_based_spawn 1 now, if enabled.
set k_ctf_runes 0
