#define BUILD_NUMBER "$Revision$"

// Everything under this comment can be deleted anytime, since the build process will each time add a space to that file, to make sure it gets commited every time in order to update the build number using the SVN file properties (keywords).
                                             
          